on:
  push:
    branches:
      - master







jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Testing Github Actions'
        run: |pwd

      - name: 'Testing Printing'
        run: echo Mohamed
